# Custom Components

Project-owned components live here. These components can compose and customize
the primitives from `components/ui`, but they should not be treated as generated
shadcn files.

Keep `components/ui` focused on shadcn primitives so CLI updates and future
component installs have a clear boundary.
