@AGENTS.md

# Design rules
- **Never use CSS gradients.** Do not use `bg-gradient-*`, `from-*`, `via-*`, or `to-*` Tailwind classes anywhere in this project. Use solid colors only.
- Keep icons minimal — only use them where they carry clear meaning, not as decoration beside every label.
